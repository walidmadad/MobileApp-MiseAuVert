<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo_miseauvert"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_miseauvert_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiseAuVert"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".Vue.ModifierAnimal"
            android:exported="false" />

        <activity
            android:name=".Vue.SupprimerAnimal"
            android:exported="false" />
        <activity
            android:name=".Vue.AnimalAmodifierr"
            android:exported="false" />
        <activity
            android:name=".Vue.CreationCompte"
            android:exported="false" />
        <activity
            android:name=".Vue.AjouterAnimal"
            android:exported="false" />
        <activity
            android:name=".Vue.GestionCompte"
            android:exported="false" />
        <activity
            android:name=".Vue.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>